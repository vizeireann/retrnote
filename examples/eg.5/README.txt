In this example, eg.5.txt contains the raw input text with HTML tags - <h1>, <h2>, <h3>, and <p> and also shows the usage of <b> Bold, <i> Italic, and <u> Underline tags, with some examples of tag nesting.

The output file eg.5.docx is generated using the following command:

retrnote eg.5.txt eg.5.docx --font "Alegreya"
